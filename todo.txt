TODOs:
3. logging for gpio lab
    3a. plus some more tests too (see below)
4. start work on next lab, either timers & atomic or threads & kernel events 

more gpio tests??
- test the action LEDs are actually out of phase with each other
- test buttons (besides reset) are disabled in error state
- phase restoration after sleep





PLAN:
march 19 - 26 = threads and kernel events && state machine framework
march 26 - april 2 = split adc & pwm (amelia work on one, sophie work on one)
april 2 - 9 = ecg ble temp
april 9 - 16 = ecg ble temp
april 16 - 23 = want to be done by 23rd but this week will be a buffer since its final exam period
april 23 - 30 = time for stuff at the end!!

IF WE HAVE TIME AT THE END:
	⁃	makefile for syntax checks (03-19)
	⁃	make framework more generic (03-19)
	⁃	timers lab logging stuff
	⁃	i2c emulation for temperature sensor (zephyr built-in!!)


Lab order:
    - gpio (one tag)--> DONE!!!!!!
    - timers and atomic operations (one tag) (we don’t have this)
    - threads and kernel events (three tags)
    - state machine framework (one tag)
    - adc (three tags)
    - pwm (two tags)
    - ecg ble temp (one tag / v4.0.+)
